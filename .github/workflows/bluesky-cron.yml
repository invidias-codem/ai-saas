name: Bluesky Agent Cron

on:
  workflow_dispatch:

jobs:
  cron-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Bluesky cron temporarily disabled
        run: |
          echo "Bluesky cron is temporarily disabled pending dry-run safety deployment and host correction."
