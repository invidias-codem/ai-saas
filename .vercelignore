# Exclude Firebase/Cloud Functions (has its own node_modules with symlinks)
/functions/

# Exclude anycrawl subproject (has its own node_modules with symlinks)
/anycrawl/

# Exclude remote/mobile subproject (has its own node_modules with symlinks)
/remote/

# Exclude legacy/generated UCOL node package only
/packages/ucol-node/

# Exclude Go harness source tree from root-relative paths; binary is still
# needed at runtime by `lib/harness/GoIOHarness.ts`, so we keep it under
# Vercel deployment by not ignoring the top-level `go-harness/` directory.
# /go-harness/

# Exclude Firebase/Cloud Functions (has its own node_modules with symlinks)
/functions/
**/__pycache__/

# Exclude Tauri build artifacts
/src-tauri/target/

# Exclude AI agent skill directories (root .agent only)
/.agent/
